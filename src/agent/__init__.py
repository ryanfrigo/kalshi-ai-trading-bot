"""Agentic trading toolbelt — the tools Claude drives on each /loop tick.

The agent (Claude) is at the helm: it assesses state, scans for edge, researches
true probabilities, decides with discipline, executes under the risk governor,
journals every prediction, and measures its own calibration over time.
"""
